<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    tools:context=".StartActivity" 
    android:background="#64ffffff"
    >
    <TextView android:id="@+id/time_catalog" android:paddingRight="4.0dip" android:layout_height="wrap_content" style="@style/MMListCatalog" />
	
	<RelativeLayout 
	    android:layout_width="fill_parent"
       android:layout_height="wrap_content"
       android:layout_below="@+id/time_catalog"
	    >
	    <RelativeLayout 
	    android:layout_width="fill_parent"
       android:layout_height="wrap_content"
       android:layout_marginTop="5dip"
       android:layout_marginBottom="5dip"
       android:layout_centerInParent="true"
	    >
	<ImageView 
	    android:layout_width="70dip"
	    android:layout_height="70dip"
	    android:src="@drawable/ic_launcher"
	    android:layout_centerVertical="true"
	    android:layout_alignParentRight="true"
	    android:id="@+id/notification_img"
	    android:layout_marginRight="5dip"
	    />
   <TextView 
       android:id="@+id/notification_text"
       android:layout_width="fill_parent"
       android:layout_height="wrap_content"
       android:layout_toLeftOf="@+id/notification_img"
       android:layout_marginRight="5dip"
       android:text="公告标题"
       android:maxLines="2"
       android:layout_centerVertical="true"
       android:ellipsize="end"
       />
   </RelativeLayout>
</RelativeLayout>
</RelativeLayout>